import React, { useState, useEffect } from "react";

const Loading = () => {
  let content = (
    <React.Fragment>
      <h1 className="loading">LOADING</h1>
    </React.Fragment>
  );

  return content;
};

export default Loading;
